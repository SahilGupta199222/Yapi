<?xml version="1.0" encoding="utf-8"?>
<layout>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tvShareFileTitle"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/share_this_file"
        android:textSize="@dimen/normalTextSize"
        android:layout_width="wrap_content"
        android:textColor="@color/darkGrey"
        android:layout_height="wrap_content"/>

    <ImageView
        android:id="@+id/ivCrossOutsideLogout"
        android:layout_width="@dimen/iconSize"
        android:layout_height="@dimen/iconSize"
        android:layout_marginStart="@dimen/_5sdp"
        android:padding="@dimen/_2sdp"
        android:src="@drawable/ic_cross_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tvShareFileTitle"
        app:layout_constraintBottom_toBottomOf="@id/tvShareFileTitle"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>